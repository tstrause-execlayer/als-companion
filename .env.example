# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will NOT be committed.

# Your Google Generative AI (Gemini) API key — ONLY for local development.
# In production, set VITE_API_BASE_URL to your backend URL instead.
VITE_GOOGLE_API_KEY=AIza...

# In production, point this to your own backend API proxy endpoint.
# The proxy should forward requests to Google/Anthropic so the key stays server-side.
# Example: https://your-backend.vercel.app
# If this is set, the app will call ${VITE_API_BASE_URL}/api/chat instead of the direct provider API.
VITE_API_BASE_URL=
