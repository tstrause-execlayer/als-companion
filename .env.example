# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will NOT be committed.

# Your Anthropic API key — ONLY for local development.
# In production, set VITE_API_BASE_URL to your backend URL instead.
VITE_ANTHROPIC_API_KEY=sk-ant-...

# In production, point this to your own backend API proxy endpoint.
# The proxy should forward requests to Anthropic so the key stays server-side.
# Example: https://your-backend.vercel.app
# If this is set, the app will call ${VITE_API_BASE_URL}/api/chat instead of Anthropic directly.
VITE_API_BASE_URL=
